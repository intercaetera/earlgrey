import Config

config :nostrum,
  token: "<bot token here>",
  num_shards: :auto

config :earlgrey,
  confirmation_thread: "<link to confirmation thread here>",
  posting_channel_id: 0 # channel in which bot is supposed to post
